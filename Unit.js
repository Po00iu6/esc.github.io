// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"ph\" id=\"h_1\" onclick=\"window.location.href=\'./index.html\'\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_1');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "NOI");








// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"ph\" id=\"h_2\" onclick=\"window.location.href=\'./PH.html\'\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_2');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "物理");








// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"ph\" id=\"h_3\" onclick=\"window.location.href=\'./CM.html\'\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_3');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "化学");








// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"ph\" id=\"h_4\" onclick=\"window.location.href=\'./BO.html\'\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_4');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "生物");








// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"wj\" id=\"h_5\" onclick=\"window.open(\'https://oi-wiki.org/\')\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_5');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "OIWiki");








// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"wj\" id=\"h_6\" onclick=\"window.open(\'https://www.luogu.com.cn/\')\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_6');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "洛谷");








// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"wj\" id=\"h_7\" onclick=\"window.open(\'https://www.bilibili.com/\')\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_7');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "Bilibili");








// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"wj\" id=\"h_8\" onclick=\"window.open(\'https://contest.xinyoudui.com/contest-list\')\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_8');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "信友队");








// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"wj\" id=\"h_9\" onclick=\"window.open(\'https://ac.nowcoder.com/acm/contest/vip-index\')\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_9');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "牛客竞赛");








// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"wj\" id=\"h_10\" onclick=\"window.open(\'https://atcoder.jp/contests/\')\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_10');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "AtCoder");








// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"wj\" id=\"h_11\" onclick=\"window.open(\'https://www.noi.cn/\')\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_11');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "NOI");








// 获取要插入的位置的父元素
var parentElement = document.getElementById('_header');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "<div class=\"wj\" id=\"h_12\" onclick=\"window.open(\'https://codeforces.com/contests\')\">");


// 获取要插入的位置的父元素
var parentElement = document.getElementById('h_12');

// 在 parentElement 的末尾插入新的 HTML 字符串
parentElement.insertAdjacentHTML('beforeend', "Codeforces");